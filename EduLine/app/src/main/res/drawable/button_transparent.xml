<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="1.2dp"
        android:color="@color/accent_light" /> <!-- Black color for the border -->
    <corners android:radius="4dp" />
</shape>